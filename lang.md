<!-- 本文件维护适用于所有语言的通用代码习惯。 -->

# 跨语言代码习惯

- 避免深层 `if`、`for`、`switch` 嵌套；但 `if a && b || (c && d) && ...` 这类超长复合条件，拆成嵌套的多层 `if` 更易读，此时可读性优先于嵌套深度。
- 优先提前返回、提前 `continue`、拆小函数，让主流程清晰。
- 优先使用语言内置能力、标准库和项目已有 helper；不够时再选成熟、维护良好、项目能接受的第三方库。
- 不为简单逻辑新增依赖，也不手写已有稳定实现的通用能力。
- 注释多少随复杂度调整：方法要有整体说明；方法内连续大段复杂逻辑（例如处理用户资料时连续 20+ 行的头像审核）要加注释说明业务意图，避免读者一眼看不出代码目的。
- 实现后尽量用测试、构建、dry-run 或最小复现验证结果，不能验证要说明原因。
