<!-- 本文件维护不同类型项目中的 Git 分支、MR 和 push 习惯。 -->

# Git 使用习惯

- git 提交时除非特殊说明，必须使用中文
- git 提交时拆分辅助函数、不同功能的修改
- git 提交时禁止使用`Commit Types`，简短一两句话描述重点即可
- 不得在 `dev`、`stage`、`pre`、`master`、`main`、`release` 等具有独特意义的分支直接提交
- 新 git 项目使用 `master` 作为主分支名，禁止使用 `main`
